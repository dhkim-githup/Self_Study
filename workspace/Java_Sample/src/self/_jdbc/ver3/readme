# JDBC Ver2

# Java class 하나로 

# 요청접수 : Controller

>> # 요청처리 : Service => Service 부분을 별도 클래스로 분리한다.

#### DB 접속 : Dao => DB 접속, 처리 부분을 별도의 공통 모듈로 분리한다.

# 데이타 저장 : entity

>> # 데이타 변환 : DTO (VO) => 데이타를 저장하고 값을 리턴한다.

를 모두 처리한다.